//Faça um programa que receba às duas dimensões de um cômodo (em metros), calcule e apresente a sua área e a potência de iluminação que deverá ser utilizada.

#include <stdio.h>
#include <windows.h>
#include <math.h>

int main(){
    UINT CPAGE_UTF8 = 65001;
    SetConsoleOutputCP (CPAGE_UTF8);

    int a=0, b=0, c=0, d=0, e=0;

    if(a||b){
        printf("comando 1");
        if(c && d)
            printf("comando2");
        else{
            if(e)
                prinft("comando3");
        }   else{
            prinft("comando4")
            if(c)
                prinft("comando5");
        }
        if(d)
            prinft("comando6");
        prinft("comando7")
    }

}
else{
        prinft("comando8");
        if(c && d)
            prinft("comando9");
}